name: notitie_applicatie
description: Onze notite applicatie A3B

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  hive: ^2.0.4
  hive_flutter: ^1.0.0
  flutter_slidable: ^0.6.0
  dart_date: ^1.1.0
  # For OS-specific directory paths
  path_provider: ^2.0.2
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^1.1.0
  build_runner: ^2.0.4

flutter:
  uses-material-design: true
